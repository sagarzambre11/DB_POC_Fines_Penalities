"""
frontend/
---------
Streamlit UI components for the Regulatory Enforcement Intelligence v4 app.

Modules:
  styles   — Global CSS injected at app startup
  helpers  — Utility functions: intent detection, style maps, session helpers
  pipeline — Pipeline runner + chat response builders
  sidebar  — Sidebar: file upload, agent status, pipeline log, config
  results  — Results section: metrics banner + 3 tabs + download button
  chat     — Chat interface: history rendering + input handler
"""
